html
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Rejouer – Le Jeu Pro</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <!-- Meni navigasyon -->
  <div id="menu"></div>

  <!-- Kontni prensipal -->
  <div class="container">
    <h2>🔄 Rejouer au défi</h2>
    <p>Tu veux créer un nouveau défi avec des réponses différentes? Clique ci-dessous pour recommencer:</p>

    <button onclick="resetGame()">🧹 Réinitialiser et recommencer</button>
    <p style="margin-top: 20px;">Ou retourne à l’accueil: <a href="index.html">🏠 Accueil</a></p>
  </div>

  <!-- Footer -->
  <div id="footer"></div>

  <!-- Script pou chaje meni/footer + reset -->
  <script>
    // Chaje meni ak footer
    fetch("menu.html").then(res => res.text()).then(data => {
      document.getElementById("menu").innerHTML = data;
});
    fetch("footer.html").then(res => res.text()).then(data => {
      document.getElementById("footer").innerHTML = data;
});

    // Fonksyon pou reset tout done
    function resetGame() {
      localStorage.removeItem("username");
      localStorage.removeItem("userAnswers");
      localStorage.removeItem("scoreboard");
      localStorage.removeItem("shareLink");
      window.location.href = "questions.html";
}
  </script>
</body>
</html>
 ⁠